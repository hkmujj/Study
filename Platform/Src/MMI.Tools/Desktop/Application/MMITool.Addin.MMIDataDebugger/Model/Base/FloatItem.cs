namespace MMITool.Addin.MMIDataDebugger.Model.Base
{
    public class FloatItem : ValueItem<float>
    {
        public FloatItem(string name, int index) : base(name, index)
        {
        }
    }
}
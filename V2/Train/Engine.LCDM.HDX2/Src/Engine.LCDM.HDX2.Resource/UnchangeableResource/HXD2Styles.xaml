<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="RadiusRectText" TargetType="TextBox">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Grid>
                        <Rectangle Fill="{TemplateBinding TextBox.Background}"
                                   RadiusX="5"
                                   RadiusY="5"
                                   Stroke="{TemplateBinding Border.BorderBrush}"
                                   StrokeThickness="{TemplateBinding Border.BorderThickness}" />
                        <TextBlock Margin="{TemplateBinding TextBox.Padding}"
                                   VerticalAlignment="Center"
                                   Text="{TemplateBinding TextBox.Text}"
                                   TextAlignment="Center" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
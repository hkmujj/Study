<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Base/TCMSLanZhouBaseResource.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="Subway.TCMS.LanZhou.BorderTextStyle" TargetType="TextBox">
        <Style.Setters>
            <Setter Property="Background" Value="{StaticResource Subway.TCMS.LanZhou.Brush.ContentBackground}" />
            <Setter Property="Foreground" Value="{StaticResource Subway.TCMS.LanZhou.Brush.ForegroundWhite}" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="IsEnabled" Value="False" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Width="{TemplateBinding ActualWidth}"
                                Height="{TemplateBinding ActualHeight}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding Text}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

    <Style x:Key="Subway.TCMS.LanZhou.ContentConstWhiteTextStyle" TargetType="TextBlock">
        <Style.Setters>
            <Setter Property="FontSize" Value="19" />
            <Setter Property="FontFamily" Value="{StaticResource FontFamilySongTypeface}" />
            <Setter Property="Foreground" Value="{StaticResource Subway.TCMS.LanZhou.Brush.ForegroundWhite}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style.Setters>
    </Style>

    <Style x:Key="Subway.TCMS.LanZhou.ContentLittleWhiteTextStyle" TargetType="TextBlock">
        <Style.Setters>
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontFamily" Value="{StaticResource FontFamilySongTypeface}" />
            <Setter Property="Foreground" Value="{StaticResource Subway.TCMS.LanZhou.Brush.ForegroundWhite}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style.Setters>
    </Style>

    <Style x:Key="Subway.TCMS.LanZhou.RunningTowBrakePercentTextStyle" TargetType="TextBlock">
        <Style.Setters>
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Background" Value="{StaticResource Subway.TCMS.LanZhou.Brush.ContentBackground}" />
            <Setter Property="Foreground" Value="{StaticResource Subway.TCMS.LanZhou.Brush.ForegroundWhite}" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
            <Setter Property="TextAlignment" Value="Center" />
        </Style.Setters>
    </Style>

    <Style x:Key="Subway.TCMS.LanZhou.FualtDetailPageBorderTextStyle" TargetType="TextBox">
        <Style.Setters>
            <Setter Property="Background" Value="{StaticResource Subway.TCMS.LanZhou.Brush.TextWrapStyleBackground}" />
            <Setter Property="Foreground" Value="{StaticResource Subway.TCMS.LanZhou.Brush.ForegroundWhite}" />
            <Setter Property="FontSize" Value="19" />
            <Setter Property="IsEnabled" Value="False" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Width="{TemplateBinding ActualWidth}"
                                Height="{TemplateBinding ActualHeight}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding Text}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

    <Style x:Key="Subway.TCMS.LanZhou.FualtDetailPageNoBackgroundBorderTextStyle" TargetType="TextBox">
        <Style.Setters>
            <Setter Property="Background" Value="{StaticResource Subway.TCMS.LanZhou.Brush.ContentBackground}" />
            <Setter Property="Foreground" Value="{StaticResource Subway.TCMS.LanZhou.Brush.ForegroundWhite}" />
            <Setter Property="FontSize" Value="19" />
            <Setter Property="IsEnabled" Value="False" />
            <Setter Property="TextAlignment" Value="Center" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Width="{TemplateBinding ActualWidth}"
                                Height="{TemplateBinding ActualHeight}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalAlignment}"
                                              VerticalAlignment="Center"
                                              Content="{TemplateBinding Text}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style.Setters>
    </Style>

</ResourceDictionary>
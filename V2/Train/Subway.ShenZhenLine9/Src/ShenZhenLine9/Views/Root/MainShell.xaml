<UserControl x:Class="Subway.ShenZhenLine9.Views.Root.MainShell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:constance="clr-namespace:Subway.ShenZhenLine9.Constance"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:regions="http://www.codeplex.com/CompositeWPF"
             d:DesignHeight="600"
             d:DesignWidth="800"
             Background="{StaticResource  Subway.ShenZhenLine9.DarkBlueBrush}"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40*" />
            <RowDefinition Height="480*" />
            <RowDefinition Height="80*" />
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" regions:RegionManager.RegionName="{x:Static constance:RegionNames.Title}" />
        <ContentControl Grid.Row="1" regions:RegionManager.RegionName="{x:Static constance:RegionNames.MainContent}" />
        <ContentControl Grid.Row="2" regions:RegionManager.RegionName="{x:Static constance:RegionNames.BottomButtom}" />
    </Grid>
</UserControl>

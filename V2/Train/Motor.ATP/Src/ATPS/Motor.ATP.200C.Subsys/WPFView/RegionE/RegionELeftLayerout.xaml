<UserControl x:Class="Motor.ATP._200C.Subsys.WPFView.RegionE.RegionELeftLayerout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:constant="clr-namespace:Motor.ATP._200C.Subsys.Constant"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Motor.ATP._200C.Subsys.WPFView.RegionE"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:regions="http://www.codeplex.com/CompositeWPF"
             d:DesignHeight="126"
             d:DesignWidth="334"
             mc:Ignorable="d">
    <Grid d:DesignHeight="126" d:DesignWidth="334">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="54*" />
            <ColumnDefinition Width="234*" />
            <ColumnDefinition Width="46*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0" d:DesignHeight="126">
            <Grid.RowDefinitions>
                <RowDefinition Height="36*" />
                <RowDefinition Height="30*" />
                <RowDefinition Height="30*" />
                <RowDefinition Height="30*" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0"
                    regions:RegionManager.RegionName="{x:Static constant:RegionNames.RegionE03}"
                    Style="{StaticResource Motor.ATP.200C.BackGridBorderStyleLeftUp}" />
            <Border Grid.Row="1"
                    regions:RegionManager.RegionName="{x:Static constant:RegionNames.RegionE04}"
                    Style="{StaticResource Motor.ATP.200C.BackGridBorderStyleLeftUp}" />
            <Border Grid.Row="2"
                    regions:RegionManager.RegionName="{x:Static constant:RegionNames.RegionE05}"
                    Style="{StaticResource Motor.ATP.200C.BackGridBorderStyleLeftUp}" />
            <Border Grid.Row="3"
                    regions:RegionManager.RegionName="{x:Static constant:RegionNames.RegionE01}"
                    Style="{StaticResource Motor.ATP.200C.BackGridBorderStyleLeftUp}" />
        </Grid>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="100*" />
                <RowDefinition Height="26*" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0"
                    regions:RegionManager.RegionName="{x:Static constant:RegionNames.RegionETextInfo}"
                    Style="{StaticResource Motor.ATP.200C.BackGridBorderStyleLeftUp}" />
            <Border Grid.Row="1"
                    regions:RegionManager.RegionName="{x:Static constant:RegionNames.RegionE23}"
                    BorderBrush="{StaticResource Motor.ATP.200C.WhiteBrush}"
                    BorderThickness="2" />
        </Grid>

        <Border Grid.Column="2" regions:RegionManager.RegionName="{x:Static constant:RegionNames.RegionEMsgArraw}" />
    </Grid>

</UserControl>

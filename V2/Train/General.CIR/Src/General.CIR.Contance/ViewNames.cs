using General.CIR.Views.ColumnEnd;
using General.CIR.Views.MasterInfo;
using General.CIR.Views.Screen;

namespace General.CIR.Contance
{
	public class ViewNames
	{
		public static readonly string BlackScreenView = typeof(BlackScreenView).FullName;

		public static readonly string StartingView = typeof(StartingView).FullName;

		public static readonly string MainContent = typeof(MainContent).FullName;

		public static readonly string ColumnEndOne = typeof(ColumnEndOne).FullName;

		public static readonly string MasterInfoNull = typeof(MasterInfoNull).FullName;

		public static readonly string CallingView = typeof(CallingView).FullName;

		public static readonly string InCommingCallView = typeof(InCommingCallView).FullName;

		public static readonly string CnterInfoView = typeof(CenterInfoView).FullName;

		public static readonly string SettingView = typeof(SettingView).FullName;
	}
}

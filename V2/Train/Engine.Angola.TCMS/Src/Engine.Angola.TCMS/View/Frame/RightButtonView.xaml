<UserControl x:Class="Engine.Angola.TCMS.View.Frame.RightButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModel="clr-namespace:Engine.Angola.TCMS.ViewModel"
             d:DataContext="{d:DesignInstance viewModel:AngolaTCMSShellViewModel}"
             d:DesignHeight="600"
             d:DesignWidth="80"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Button Command="{Binding RightBtnViewModel.Model.F1Command}"
                Content="F1"
                Style="{StaticResource FrameButtonStyle}" />
        <Button Grid.Row="1"
                Command="{Binding RightBtnViewModel.Model.F2Command}"
                Content="F2"
                Style="{StaticResource FrameButtonStyle}" />
        <Button Grid.Row="2"
                Command="{Binding RightBtnViewModel.Model.F3Command}"
                Content="F3"
                Style="{StaticResource FrameButtonStyle}" />
        <Button Grid.Row="3"
                Command="{Binding RightBtnViewModel.Model.F4Command}"
                Content="F4"
                Style="{StaticResource FrameButtonStyle}" />
        <Button Grid.Row="4"
                Command="{Binding RightBtnViewModel.Model.F5Command}"
                Content="F5"
                Style="{StaticResource FrameButtonStyle}" />

    </Grid>
</UserControl>

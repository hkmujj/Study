<UserControl x:Class="Motor.ATP._200C.Subsys.WPFView.PopupViews.Contents.SelectBrakeTestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="250"
             d:DesignWidth="232"
             Background="{StaticResource Motor.ATP.200C.Bursh.PopViewContent}"
             mc:Ignorable="d">
    <StackPanel Margin="50,10,0,0">
        <TextBlock Style="{StaticResource Motor.ATP.200C.PopupViewContentTextStyle}">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0}{1}">
                    <Binding Source="{StaticResource Motor.ATP.Infrasturcture.String.EmergencyBrake}" />
                    <Binding Source="{StaticResource Motor.ATP.Infrasturcture.String.Test}" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
        <TextBlock Style="{StaticResource Motor.ATP.200C.PopupViewContentTextStyle}">
            <TextBlock.Text>
                <MultiBinding StringFormat="{}{0}{1}{2}">
                    <Binding Source="{StaticResource Motor.ATP.Infrasturcture.String.Max}" />
                    <Binding Source="{StaticResource Motor.ATP.Infrasturcture.String.NormalBrake}" />
                    <Binding Source="{StaticResource Motor.ATP.Infrasturcture.String.Test}" />
                </MultiBinding>
            </TextBlock.Text>
        </TextBlock>
    </StackPanel>
    <!--<Grid Margin="50">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Style="{StaticResource Motor.ATP.200C.PopupViewContentTextStyle}" Text="{StaticResource Motor.ATP.Infrasturcture.String.PupContentRunBrakeTest}" />
        <TextBlock Grid.Row="1"
                   Style="{StaticResource Motor.ATP.200C.PopupViewContentTextStyle}"
                   Text="{StaticResource Motor.ATP.Infrasturcture.String.PupContentSkipBrakeTest}" />
    </Grid>-->
</UserControl>

<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="clr-namespace:Engine.TCMS.Turkmenistan.View.Common">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="../Base/TurkmenistanBaseResource.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style x:Key="ProgressBarOrdinaryStyle" TargetType="ProgressBar">
        <Setter Property="Orientation" Value="Vertical" />
        <Setter Property="Background" Value="{DynamicResource BlackBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{DynamicResource MainTextBrushOne}" />
        <Setter Property="Foreground" Value="{DynamicResource ContentDeepSkyBlueBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ProgressBar">
                    <Border
                        Name="border"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                        <Grid>
                            <Rectangle Name="PART_Track" Fill="{TemplateBinding Background}" />
                            <Rectangle
                                Name="PART_Indicator"
                                HorizontalAlignment="Left"
                                Fill="{TemplateBinding Foreground}" />
                        </Grid>
                    </Border>

                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter TargetName="border" Property="LayoutTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="270" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>

                        <Trigger Property="IsIndeterminate" Value="True">
                            <Setter TargetName="PART_Indicator" Property="Style">
                                <Setter.Value>
                                    <Style TargetType="FrameworkElement">
                                        <Setter Property="HorizontalAlignment" Value="Stretch" />
                                        <Style.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <ColorAnimation
                                                            AutoReverse="True"
                                                            RepeatBehavior="Forever"
                                                            Storyboard.TargetProperty="Fill.Color"
                                                            To="Transparent"
                                                            Duration="0:0:0.5" />
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="MainViewBorderStyle" TargetType="common:EffectBorder">
        <Setter Property="LineWidth" Value="5" />
        <Setter Property="Line1Fill" Value="#807E86" />
        <Setter Property="Line2Fill" Value="#A8A3A8" />
        <Setter Property="Margin" Value="10,2" />
    </Style>
    <Style x:Key="MainViewBorderStyle2" TargetType="common:EffectBorder">
        <Setter Property="LineWidth" Value="5" />
        <Setter Property="Background" Value="{DynamicResource BlackBrush}" />
        <Setter Property="Line1Fill" Value="#807E86" />
        <Setter Property="Line2Fill" Value="#A8A3A8" />
        <Setter Property="FontSize" Value="{DynamicResource Double20}" />
        <Setter Property="Margin" Value="2,3" />
    </Style>

    <Style x:Key="MainBorderStyle" TargetType="Border">
        <Setter Property="Background" Value="#6B6769" />
        <Setter Property="BorderBrush" Value="#7E8189" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="BorderThickness" Value="3" />
    </Style>
    <Style x:Key="MainBorderStyle2" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource BackgroudBrush}" />
        <Setter Property="BorderBrush" Value="#7E8189" />
        <Setter Property="Margin" Value="2" />
        <Setter Property="BorderThickness" Value="3" />
    </Style>
</ResourceDictionary>